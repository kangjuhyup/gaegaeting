apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: match
  namespace: app
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    # Attach Traefik ForwardAuth middleware (namespace-name@kubernetescrd)
    # This will call auth service and, if present, propagate `x-jwt-payload` to the match upstream request.
    traefik.ingress.kubernetes.io/router.middlewares: app-match-forwardauth@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - api.gaegaeting.app
      secretName: match-tls
  rules:
    - host: api.gaegaeting.app
      http:
        paths:
          - path: /match
            pathType: Prefix
            backend:
              service:
                name: match
                port:
                  number: 3000
