apiVersion: v1
kind: Service
metadata:
  name: mysql-master
  namespace: database
  labels:
    app: mysql
    component: master
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
      protocol: TCP
  selector:
    app: mysql
    component: master
