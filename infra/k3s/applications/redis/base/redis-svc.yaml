apiVersion: v1
kind: Service
metadata:
  name: redis-master
  namespace: database
  labels:
    app: redis
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
  selector:
    app: redis
