apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-gaegaeting-app
  namespace: istio-system
spec:
  secretName: api-gaegaeting-app-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - api.gaegaeting.app
