apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-gaegaeting-app
  namespace: istio-system
spec:
  secretName: argo-gaegaeting-app-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - argo.gaegaeting.app
