apiVersion: v1
kind: Service
metadata:
  name: account
  namespace: app
  labels:
    app: account
spec:
  type: ClusterIP
  ports:
    - port: 2800
      targetPort: 2800
      protocol: TCP
      name: http
  selector:
    app: account
